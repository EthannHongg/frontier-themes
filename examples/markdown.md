# Markdown sample — heading hierarchy, lists, code, quotes

# Heading 1 — brand primary

Intro paragraph with **bold**, *italic*, and `inline code`. A [link](https://example.com) for underline styling.

## Heading 2 — info tone

### Heading 3 — type tone

#### Heading 4 — function tone

##### Heading 5 — string tone

###### Heading 6 — attribute tone

> Blockquote with *emphasis* and a list inside:
>
> - Item one
> - Item two

1. Ordered list
2. Second item
   - Nested bullet

```typescript
// Fenced code block
export function sum(a: number, b: number): number {
  return a + b;
}
```

| Column A | Column B |
|----------|----------|
| `code`   | **bold** |

~~deleted text~~ and ==inserted text== (if supported by grammar).

---

**Changed:** syntax refresh · **Added:** semantic tokens · **Removed:** flat 16-rule palette
